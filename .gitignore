# Python
__pycache__/
*.py[cod]
.pytest_cache/

# uv / venv
.venv/

# macOS
.DS_Store

# Large media + run artifacts
videos/
outputs/
verify_frame.jpg
*.mp4
*.jpg

# Camera zone files (per-camera config)
zones/

# Per-camera generated run scripts + run outputs (reference gitignored zones/)
run.sh
viewscripts/
runs/

# Default-named run outputs at the repo root (run.sh / --wait-log / --busy-log)
waits.csv
busy.csv

# Per-camera generated busy calibration (tied to gitignored videos/, like zones/)
calib/
